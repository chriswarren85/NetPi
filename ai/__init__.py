# AI Intelligence Layer — read-only consumers of core project data.
# Nothing in this package writes directly to devices.json, topology.json,
# multicast_groups.json, or validation state. All output is returned as
# structured JSON for the caller to present or optionally persist.
